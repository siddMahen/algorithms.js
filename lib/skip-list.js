var linkedList = require("./linked-list");

/*
 * A sucession of sparser and sparser linked lists.
 *
 * Supports insert, delete and search operations in O(lgn)
 *
 */

var skipList = function(){
    this.lists = [new linkedList()];
}

skipList.prototype.search = function(key){

}

skipList.prototype.insert = function(key, val){

}

skipList.prototype.delete = function(key){

}
